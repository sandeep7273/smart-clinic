name: Terraform — Plan & Apply

on:
  push:
    branches: [main, master, develop]
    paths:
      - "infrastructure/terraform/**"
      - ".github/workflows/terraform.yml"
  pull_request:
    branches: [main, master, develop]
    paths:
      - "infrastructure/terraform/**"
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "dev"
        type: choice
        options: [dev, prod]
      action:
        description: "Terraform action"
        required: true
        default: "plan"
        type: choice
        options: [plan, apply, destroy]

env:
  TF_VERSION: "1.6.6"
  ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}

jobs:
  terraform:
    name: Terraform ${{ github.event.inputs.action || (github.event_name == 'pull_request' && 'plan') || 'apply' }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write # post plan output as PR comment

    defaults:
      run:
        working-directory: infrastructure/terraform

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Terraform Init
        run: terraform init -reconfigure

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Generate terraform.tfvars from secrets
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: |
          # Derive per-service MongoDB URIs from base URI
          # Base: mongodb+srv://user:pass@cluster.mongodb.net
          # Append database name and appName per service
          BASE="${MONGODB_URI%%\?*}"   # strip query params
          QP="${MONGODB_URI#*\?}"      # keep query params (e.g. appName=Cluster0)

          mkdir -p environments/${{ env.ENVIRONMENT }}
          cat > environments/${{ env.ENVIRONMENT }}/terraform.tfvars << EOF
          environment        = "${{ env.ENVIRONMENT }}"
          aws_region         = "${{ secrets.AWS_REGION }}"
          project            = "smartclinic"
          vpc_cidr           = "10.0.0.0/16"
          availability_zones = ["ap-south-1a", "ap-south-1b"]
          acm_certificate_arn = ""

          api_gateway_image         = "PLACEHOLDER"
          auth_service_image        = "PLACEHOLDER"
          doctor_service_image      = "PLACEHOLDER"
          appointment_service_image = "PLACEHOLDER"
          ai_service_image          = "PLACEHOLDER"

          jwt_secret   = "${JWT_SECRET}"
          groq_api_key = "${GROQ_API_KEY}"

          mongodb_uris = {
            "auth-service"        = "${BASE}/auth_db?${QP}"
            "doctor-service"      = "${BASE}/doctor_db?${QP}"
            "appointment-service" = "${BASE}/appointment_service?${QP}"
            "ai-service"          = "${BASE}/ai_db?${QP}"
          }

          api_gateway_min_tasks          = 1
          api_gateway_max_tasks          = 3
          auth_service_min_tasks         = 1
          auth_service_max_tasks         = 2
          doctor_service_min_tasks       = 1
          doctor_service_max_tasks       = 4
          appointment_service_min_tasks  = 1
          appointment_service_max_tasks  = 6
          ai_service_min_tasks           = 1
          ai_service_max_tasks           = 4
          alert_email = ""
          EOF

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var-file="environments/${{ env.ENVIRONMENT }}/terraform.tfvars" \
            -out=tfplan \
            -no-color 2>&1 | tee plan-output.txt

      - name: Post plan output to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('infrastructure/terraform/plan-output.txt', 'utf8');
            const maxLength = 60000;
            const truncated = plan.length > maxLength
              ? plan.substring(0, maxLength) + '\n\n... (truncated)'
              : plan;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Terraform Plan — \`${{ env.ENVIRONMENT }}\`\n\`\`\`hcl\n${truncated}\n\`\`\``
            });

      - name: Terraform Apply
        if: |
          (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: terraform apply -auto-approve tfplan

      - name: Terraform Destroy (manual only — requires explicit trigger)
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
        run: |
          terraform destroy \
            -var-file="environments/${{ env.ENVIRONMENT }}/terraform.tfvars" \
            -auto-approve
